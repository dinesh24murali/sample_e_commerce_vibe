# Task 24: Customer Frontend Project Setup

## Priority: High
## Status: Pending
## Subagent: @react-frontend-builder

## Description
Initialize the Customer-facing React SPA with proper folder structure, routing, and Redux store setup.

## Requirements
- Create the project inside a `customer/` folder at the repo root
- Use React 18+ with functional components and hooks
- Set up Redux Toolkit for state management
- Set up React Router for navigation
- Set up project folder structure: `src/{components,pages,hooks,services,utils,context,types,assets,styles}/`
- `components/ui/` for base UI components, `components/layout/` for Header/Footer
- Configure API base URL via environment variable
- Set up axios or fetch wrapper for API calls

## Design Patterns
- Container vs Presentational Components
- Redux Toolkit Slices
